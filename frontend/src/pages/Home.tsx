import React, { useEffect, useState } from 'react';
import { api } from '../services/api';

export const Home: React.FC = () => {
  return (
    <div>
      <h1>Home Page</h1>
    
    </div>
  );
};
